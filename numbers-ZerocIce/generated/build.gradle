plugins {
    id 'java-library'
}

// Configure to use the ICE classes from root directory
sourceSets {
    main {
        java {
            srcDirs = [rootProject.projectDir]
            include 'NumbersApp/**/*.java'
        }
    }
}

// Make sure the classes are available to other projects
jar {
    from sourceSets.main.output
}